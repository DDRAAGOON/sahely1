# Copy to .env and fill in. .env is git-ignored and must never be committed.
# The app reads its API host from EnvConfig (lib/core/config/app_env.dart);
# these values are consumed by the backend and by native SDK configuration.
API_BASE_URL=https://apisahely.staysahely.com
PAYMOB_API_KEY=
TUYA_CLIENT_ID=
TUYA_CLIENT_SECRET=
FIREBASE_API_KEY=
GOOGLE_MAPS_API_KEY=
