<?xml version="1.0" encoding="utf-8"?>
<!--
  Sahely network policy.

  Every API call is HTTPS, so cleartext is refused app-wide. Debug builds may
  talk to a local backend over http (10.0.2.2 is the Android emulator host),
  which is why that exception lives in debug-overrides and never ships in a
  release build.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <!-- Lets a debug build trust a locally installed proxy CA. -->
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
